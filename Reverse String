class Solution:
	def reverseString(self, A):
        stack = []
        for ch in A:
            stack.append(ch)
        
        res = ""
        while stack:
            res += stack.pop()
        
        return res
        
''' time complexity : O(n)        
    space complexity : O(n)
'''
